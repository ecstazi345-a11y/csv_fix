import streamlit as st

st.set_page_config(layout="wide")

st.title("Доктрина RCC")
st.caption("Что это за система, зачем она создается и как мы видим её архитектуру.")

st.markdown(
    """
## Суть страницы

Здесь зафиксирована основная логика системы.

Эта система создается не как обычный BI-дашборд, а как
**операционная система управления реальностью проекта**.

Основная цепочка:

**DATA → ДОПУСК → ИСПОЛНЕНИЕ → ПРИЗНАНИЕ → ДЕНЬГИ**

## Что здесь должно быть
- смысл системы
- контуры управления
- роль Streamlit
- роль Supabase
- роль AI-сотрудников
- ключевые модули системы

## Для чего нужна страница
Чтобы при открытии проекта сразу понимать:
- что мы строим
- почему это не просто визуализация
- как данные должны превращаться в управленческие решения
"""
)

st.divider()

st.subheader("Ключевая формула")
st.info(
    "Streamlit = интерфейс | Supabase = логика и расчёты | AI = объяснение, сигналы и действия"
)

st.subheader("Основные контуры")
st.markdown(
    """
1. **Допуск** — что реально можно запускать  
2. **Исполнение** — что реально выполняется  
3. **Признание** — что стало предъявляемым и признанным  
4. **Деньги** — что стало экономикой и кэшем  
5. **AI-слой** — кто анализирует, объясняет и потом действует
"""
)

st.subheader("7 главных вопросов системы")
st.markdown(
    """
- Что реально допущено?
- Что реально исполнимо?
- Что реально выполнено?
- Что подтверждено доказательствами?
- Что признано?
- Что стало деньгами?
- Где деньги сгорают?
"""
)
import pandas as pd
from services.queries import get_rcc_home_summary

st.divider()
st.subheader("Техническая проверка: связь с Supabase")

try:
    data = get_rcc_home_summary()
    df = pd.DataFrame(data)

    if df.empty:
        st.warning("Данные пришли, но таблица пустая.")
    else:
        st.success("Связь с Supabase работает")

        st.metric("Количество строк", len(df))

        st.dataframe(df, use_container_width=True)

except Exception as e:
    st.error("Ошибка подключения")
    st.code(str(e))
